---
interface Props {
  src: string;
  alt: string;
  className: string;
}

const { src, alt, className } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class:list={[className, 'dark:brightness-0 dark:invert dark:filter']}
/>
