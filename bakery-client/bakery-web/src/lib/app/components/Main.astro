---
import { cn } from '@lib/utils';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<main
  class={cn(
    'container flex flex-col items-center justify-center px-4',
    className
  )}
>
  <slot />
</main>
