---
import { Menu } from 'lucide-react';
import { Button } from '@/components/ui/button';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Button variant="ghost" size="icon" className={`${className} cursor-pointer`}>
  <Menu className="size-fit" />
</Button>
