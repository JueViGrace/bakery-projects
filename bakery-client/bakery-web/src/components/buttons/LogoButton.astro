---
import LogoIcon from '../icons/LogoIcon.astro';
import LinkButton from './LinkButton.astro';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<LinkButton id="logo-button" href="/" className={className}>
  <LogoIcon />
</LinkButton>
