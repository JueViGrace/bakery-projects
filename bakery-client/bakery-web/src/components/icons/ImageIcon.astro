---
import { Image } from 'astro:assets';

interface Props {
  src: ImageMetadata;
  alt: string;
  className?: string;
}

const { src, alt, className } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  class:list={[className, 'dark:brightness-0 dark:invert dark:filter']}
/>
