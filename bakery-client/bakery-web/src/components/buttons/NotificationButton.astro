---
import BellSVG from '@/assets/bell.svg';
import { Button } from '@/components/ui/button';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<Button size="icon" variant="ghost" className={`${className} cursor-pointer`}>
  <BellSVG class="size-fit" />
</Button>
