---
import { Image } from 'astro:assets';
import ChefImg from '../../assets/chef.png';

interface Props {
  className?: string;
}

const { className } = Astro.props;

export const prerender = true;
---

<Image loading="eager" class={className} src={ChefImg} alt="logo" />
